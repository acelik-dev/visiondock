VisionDockComputer Vision PlatformWorkspaceHomeDiscovery & ProjectsModel LibraryDataset LibraryInferenceBilling & PlansActive projectWorksite CCTV Object Detection - jumper & canopyprj-03a8ace7✓Step 1 · Task2Step 2 · Dataset3Step 3 · TrainingProjectsprj-03a8ace736creditsadmin@visiondock.localSign out2Add training photosEach photo needs an answer already. VisionDock does not label for you — it learns from the examples you provide.What your task expectsUpload a labeled package (ZIP) where boxes are drawn around the objects in each photo.jumpercanopyFixed CCTV camera overlooking a worksitedaytimeTask: Find several objectsEasiest: use a ready datasetOpen the Dataset Library and pick a prepared set — no file packing needed.How do I prepare a labeled package?Download a tiny example packageSupported toolsWhat's leftLabeled package uploadedPackage checked and readyWhy a ZIP (not per-group photo upload)?This task is object detection — each photo needs drawn boxes around jumper, canopy, Fixed CCTV camera overlooking a worksite, daytime. Classification can use one folder per group; detection needs a labeled package from a labeling tool (YOLO / COCO / VOC).Upload your labeled packageOne ZIP where boxes are drawn around the objects in each photo.Labeled package (ZIP)No package yetExport from your labeling tool, then upload the ZIP hereYour photos(Find several objects)Photos0Boxes found—Object types—Add your photos to see a summary here.BackStill missing stepsReview setupStep 2: Upload your pre-labeled find several objects dataset.
