VisionDockComputer Vision PlatformWorkspaceHomeDiscovery & ProjectsModel LibraryDataset LibraryInferenceBilling & PlansActive projectWorksite CCTV Object Detection - jumper & canopyprj-03a8ace71Step 1 · Task2Step 2 · Dataset3Step 3 · TrainingProjectsprj-03a8ace736creditsadmin@visiondock.localSign out1Define Your Vision TaskUpload 10 sample photos, then chat with AI to shape your project+710/10 ✓Thanks for the sample photos — I'm taking a look…07:29 PMI see two rectangular panels with solid fill colors and small sample IDs in the corner: one green panel labeled "sample-3" and one gray panel labeled "sample-9." Most likely task: image classification (one category per image), though object localization could fit if you need boxes around the panels. Quick questions: 1) What labels, categories, or numeric targets do you already have in your dataset? (Examples I can imagine from these photos: color labels like "green" / "gray", status labels like "OK" / "defect", or a numeric measurement per sample.) 2) Where are the cameras installed — on a production line, inspection station, handheld, or elsewhere? 3) Do you need instant alerts (real-time) or is a daily review sufficient?07:29 PMYouI need object detection with bounding boxes for multiple objects per image: jumper and canopy. Find several objects, not one.07:29 PMGot it — you want object detection that finds multiple objects per image, with bounding boxes for "jumper" and "canopy." Task type: object detection. A few quick questions to finish the picture: 1) Confirm exact class names and any other classes you already have (for example: jumper, canopy — any subtypes or extra labels?). 2) Do you already have bounding-box annotations? If so, how are they stored (one file per image, one master annotations file, or something else)? 3) Where do the cameras run (production line, inspection table, handheld, drone, etc.) and do you need instant alerts or is daily review enough?07:30 PMYouFixed CCTV camera overlooking a worksite, daytime.07:32 PMConfirming: you want object detection with bounding boxes that can find multiple objects per image. I have these details so far: - Classes: jumper, canopy. - Cameras: fixed CCTV overlooking a worksite, daytime. A few quick questions to finish: 1) Are "jumper" and "canopy" the exact class names you want, or do you have subtypes (for example "jumper-small" / "jumper-large")? 2) Do you already have bounding-box annotations? If yes, how are they stored (one file per image, a single annotations file, or another format)? 3) Do you need instant alerts (real-time) or is a daily review sufficient? Answer these and I’ll summarize the final setup.07:32 PMYouNear real-time alerts. The only classes are jumper and canopy.07:34 PMHere's what I suggest: object detection - Goal: Detect multiple objects per image and return bounding boxes for each instance of the classes below. - Classes you named: jumper, canopy. - Images/cameras: fixed CCTV camera overlooking a daytime worksite. - Speed: near real-time alerts. - Expected data format: images plus bounding-box annotations for all objects in each image, provided in a common object-detection package (for example YOLO, COCO, or Pascal VOC as a ZIP). You can now use the Generate Config button.07:34 PMYouI have answered everything. Please unlock the configuration button now.07:36 PMHere's what I suggest: object detection - Goal: Detect and return bounding boxes for every instance of the classes below in each image. - Classes: jumper, canopy. - Images/cameras: fixed CCTV camera overlooking a daytime worksite. - Speed: near real-time alerts. - Expected data format: images plus bounding-box annotations for all objects in each image, packaged as a standard object-detection dataset (for example YOLO, COCO, or Pascal VOC ZIP). You can use the Generate Config button.07:36 PMAttached images:SendGenerate ConfigSend −1 · Config −1 · balance 36 creditsYour project planHideProjectWorksite CCTV Object Detection - jumper & canopyFind several objectsDetect and localize multiple objects (e.g., jumper and canopy) in daytime CCTV images using the pre-labeled images and bounding-box annotations you upload.Task typeFind several objectsFind several objects in each photoSuggested approach: YOLOv8mExpected dataset formatUpload a labeled package (ZIP) where boxes are drawn around the objects in each photo.Classes (4)jumpercanopyFixed CCTV camera overlooking a worksitedaytimeInference & API keysComplete training to deploy an inference endpoint.VisionDock does not label data — bring your pre-labeled dataset in Step 2, then start training.Image Recognitionprj-03a8ace7Continue−1 credit · balance 36
