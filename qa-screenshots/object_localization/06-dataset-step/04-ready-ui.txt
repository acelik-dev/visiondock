VisionDockComputer Vision PlatformWorkspaceHomeDiscovery & ProjectsModel LibraryDataset LibraryInferenceBilling & PlansActive projectWidget localization - overhead industrial cameraprj-24a7a46f✓Step 1 · Task✓Step 2 · Dataset3Step 3 · TrainingProjectsprj-24a7a46f33creditsadmin@visiondock.localSign out2Add training photosEach photo needs an answer already. VisionDock does not label for you — it learns from the examples you provide.What your task expectsUpload a labeled package (ZIP) where each photo has one box drawn around the object.widgetFixed industrial camera above a fixtureindoorTask: Find one objectEasiest: use a ready datasetOpen the Dataset Library and pick a prepared set — no file packing needed.How do I prepare a labeled package?Download a tiny example packageSupported toolsReady to continueLabeled package uploadedPackage checked and readyWhy a ZIP (not per-group photo upload)?This task is object detection — each photo needs drawn boxes around widget, Fixed industrial camera above a fixture, indoor. Classification can use one folder per group; detection needs a labeled package from a labeling tool (YOLO / COCO / VOC).Upload your labeled packageOne ZIP where each photo has a single box around the object.Labeled package (ZIP)localization_package.zipExport from your labeling tool, then upload the ZIP hereYour photos(Find one object)ReadyPhotos10Boxes found10Object types1Heads-upYour package labels don’t include every object type from the task yet.BackReadyReview setupStep 2: Upload your pre-labeled find one object dataset.
