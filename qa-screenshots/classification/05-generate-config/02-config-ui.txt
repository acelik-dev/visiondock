VisionDockComputer Vision PlatformWorkspaceHomeDiscovery & ProjectsModel LibraryDataset LibraryInferenceBilling & PlansActive projectPainted Metal Parts QC - Binary Classificationprj-884a6c041Step 1 · Task2Step 2 · Dataset3Step 3 · TrainingProjectsprj-884a6c0491creditsadmin@visiondock.localSign out1Define Your Vision TaskUpload 10 sample photos, then chat with AI to shape your project+710/10 ✓Thanks for the sample photos — I'm taking a look…06:42 PMI see two example images that look like simple color panels: one mostly red (labeled "sample-1") and one mostly blue (labeled "sample-2"). Each image looks like a single, dominant class. The most likely task type is image classification (one category per image), but it could also be multi-label if you tag multiple attributes per photo. I won’t finalize yet. Quick questions: 1) What labels or numeric targets do you already have in your dataset? For example, are the classes named "sample-1" and "sample-2", or simpler labels like "red" and "blue", or do you have tags like "ok" / "reject" or a numeric score (e.g., color coverage %)? 2) Where do the cameras run? On a production line, at an inspection station, handheld, or somewhere else? 3) How fast do you need results: instant alerts (real-time) or periodic review (daily/shift reports)?06:42 PMYouI need binary image classification for product QC. Each photo should get exactly one label: OK or Defect. These are top-down photos of painted metal parts.06:42 PMPerfect — thanks. I understand you want binary image classification: one label per photo, classes "OK" and "Defect", using top-down photos of painted metal parts. Two quick questions: 1) Where do the cameras run — fixed over the production line at an inspection station, at a QC bench, handheld, or somewhere else? If fixed, roughly how many camera locations? 2) How fast do you need results — instant alerts for failing parts, or periodic review (daily or per shift)? Answer those and I’ll summarize the task and expected data format.06:43 PMYouImages come from a fixed overhead camera on a factory conveyor line, about 1280x720, indoor LED lighting.06:44 PMGot it. You want binary image classification: one label per photo, classes "OK" and "Defect". Photos are top-down views of painted metal parts from a fixed overhead camera at 1280×720 with indoor LED lighting. Two quick questions: 1) Do you need instant alerts for failing parts, or is periodic review enough (daily or per shift)? 2) What is the production rate or camera frame rate—roughly how many parts or images per minute?06:45 PMYouWe need near real-time alerts (1–3 seconds per image). Labels are exactly OK and Defect — please unlock Generate Config when ready.06:46 PMHere's what I suggest: image classification - What you want: Binary product quality control. Each top-down photo of a painted metal part gets exactly one label. - Task type: image classification (one category per image). - Classes / labels: OK, Defect. - Images / cameras: Fixed overhead camera on a factory conveyor, 1280×720, indoor LED lighting. - Speed: Near real-time alerts (1–3 seconds per image). - Expected data format: Each image paired with a single label. For example, a CSV with columns [image_filename,label], or two folders named "OK" and "Defect" containing the corresponding images. You can use the Generate Config button.06:47 PMAttached images:SendGenerate ConfigSend −1 · Config −1 · balance 91 creditsYour project planHideProjectPainted Metal Parts QC - Binary ClassificationSort into one groupBinary image classification to detect OK vs Defect on top-down photos of painted metal parts; you will upload pre-labeled images organized by class.Task typeSort into one groupPut each photo in one groupSuggested approach: EfficientNet-B0Expected dataset formatPut each photo into one group (for example: OK or Defect).Classes (3)OKDefectOK or Defect. These are top-down photos of painted metal partsInference & API keysComplete training to deploy an inference endpoint.VisionDock does not label data — bring your pre-labeled dataset in Step 2, then start training.Image Recognitionprj-884a6c04Continue−1 credit · balance 91
